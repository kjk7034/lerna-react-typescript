{
  "name": "@wise/components",
  "version": "0.0.0",
  "main": "lib/index.js",
  "typings": "lib/index.d.ts",
  "private": true,
  "scripts": {
    "start:web": "react-scripts start",
    "build": "run-p build:lib",
    "build:lib": "run-p build:ts build:gulp",
    "build:ts": "tsc -p ./tsconfig.prod.json",
    "build:gulp": "gulp build",
    "build:web": "react-scripts build"
  },
  "dependencies": {
    "classnames": "^2.2.6",
    "react-loadable": "^5.5.0",
    "react-router-dom": "^5.0.1"
  },
  "devDependencies": {
    "@types/classnames": "^2.2.9",
    "@types/react-loadable": "^5.5.1",
    "@types/react-router-dom": "^4.3.5",
    "@wise/tools": "0.0.0",
    "gulp": "^4.0.2",
    "through2": "^3.0.1"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie < 11",
    "not op_mini all"
  ],
  "eslintConfig": {
    "extends": "react-app"
  },
  "babel": {
    "presets": [
      "react-app"
    ]
  }
}
